
describe('Products List', () => {
    beforeEach(() => {
        cy.visit('http://localhost:4200/products');
    });
});

it('should conatin name', function() {
    cy.visit('http://localhost:4200/products')
    
});